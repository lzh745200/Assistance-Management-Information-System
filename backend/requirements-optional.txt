# ============================================================
# 可选依赖（预测与统计）
# ============================================================
# prophet 依赖 cmdstan C++ 编译器，在某些环境（如 ARM64、CI）安装困难。
# 未安装时系统自动降级为离线模式，不影响核心功能。
# 安装方式：pip install -r requirements-optional.txt
# ============================================================
prophet==1.3.0
pytokens==0.4.1
cmdstanpy==1.3.0
stanio==0.5.1

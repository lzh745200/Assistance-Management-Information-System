# 覆盖率 100% 攻坚进度台账（2026-07-24 起）

口径：可覆盖集 100%（真正不可执行行以 `# pragma: no cover`+理由豁免；纯类型 TS 文件移出测量集）。
规则：每批窄验证+全量周期回归；pragma 须登记；禁水测试。

## 基线（2026-07-24）

| 端 | 口径 | 覆盖 | 缺口 |
|----|------|------|------|
| 后端 | coverage.py（`.coveragerc` 豁免后） | 待新基线（重建中） | 旧数据 3,278 行/176 文件（外部改动致行号漂移，以新基线为准） |
| 前端 | v8（排除 9 个纯类型文件后） | stmts 63.84% / funcs 76.90% / branches 84.44% | 25,048 语句 |

## 阶段 0：测量口径（done）

- `frontend/vitest.config.ts`：排除 9 个纯类型文件（analytics/api/components/entities/helpProject/index/models/organization/policy）。
- `backend/.coveragerc`：exclude_lines 标准集（TYPE_CHECKING/`__main__`/abstractmethod/`__repr__`/NotImplementedError）。
- **顺带修复**：`blobDownload.spec.ts` 两个成功路径用例泄漏真实 100ms 定时器，拆除 document stub 后触发 jsdom `removeChild` 类型错误（全量 2 unhandled errors，竞态时隐时现）→ 补 `vi.useFakeTimers()`+`advanceTimersByTime(100)` 确定性消除，20/20 绿 + eslint 零。

## 批次台账

| 批次 | 模块/文件 | 缺口行 | 新增用例 | 覆盖确认 | pragma | 状态 |
|------|-----------|--------|----------|----------|--------|------|
| 待录 | — | — | — | — | — | — |

## 已知风险

- 子代理配额耗尽（403）：全程主循环串行。
- 外部会话在工作区留有未提交端点改动（messages/supported_village/admin 等）：测试按其工作区现状编写，提交只含测试文件。

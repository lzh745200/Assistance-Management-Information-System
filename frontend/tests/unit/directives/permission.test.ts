import { describe, it, expect } from 'vitest'

describe('v-permission directive (SKIP)', () => {
  it('directive registered in project', () => {
    // v-permission directive exists in src/directives/permission.ts
    expect(true).toBe(true)
  })
})
